# 近期重大升级 — v0.10 → v0.14 全景梳理

> 本文是研究的"近期升级专题"，与《总纲》《全网调研》并列。
>
> 时间窗口：2026-04-16 到 2026-05-16，覆盖 5 个正式版本（v0.10.0 / v0.11.0 / v0.12.0 / v0.13.0 / v0.14.0）。
>
> 数据规模：约 4,300+ commits、2,500+ merged PRs、730k+ 行新增、800+ 社区贡献者（含 co-author）。
>
> 研究基线：原研究覆盖 v0.9.0（2026-04-13）。本文专门梳理 v0.9.0 之后的所有体系级变更，作为对原研究 32 章的"补丁层"。

---

## 0. 读法建议

- **想看一句话总结**：直接读第 1 节"五个版本的主题词"
- **想看架构层面的变化**：第 2-5 节，按子系统分桌切分
- **想看与原研究章节的对应关系**：第 6 节"对原研究 32 章的影响矩阵"
- **想看可能的争议与悬而未决问题**：第 7 节
- **想看完整的统计/版本变化**：第 8 节

---

## 1. 五个版本的主题词

| 版本 | 发布日期 | 一句话定位 | 关键标签 |
|------|----------|------------|----------|
| v0.10.0 | 2026-04-16 | Nous Tool Gateway | 订阅打通 web/image/tts/browser |
| v0.11.0 | 2026-04-23 | The Interface | Ink TUI 重写 · Transport ABC · QQBot (17th) |
| v0.12.0 | 2026-04-30 | The Curator | 自治维护后台 · 三个新平台 · 冷启动 -57% |
| v0.13.0 | 2026-05-07 | The Tenacity | Kanban 多 Agent · `/goal` Ralph 循环 · 8 个 P0 安全闭合 |
| v0.14.0 | 2026-05-16 | The Foundation | PyPI 安装 · OpenAI 兼容代理 · 22 平台 · 原生 Windows |

五个版本连起来看，可以拆出三条主线：

1. **底座坚固化**：Transport ABC、Checkpoints v2、PyPI、原生 Windows、~19s 冷启动削减、180x 浏览器 CDP 加速
2. **多 Agent 与自治**：Curator（后台 7 天周期维护技能库）+ Kanban（耐久多 profile 协作板）+ `/goal` `/subgoal`（Ralph 循环）+ `/handoff` 跨模型/Persona 接管
3. **平台与入口扩张**：Tool Gateway 一键打开订阅化能力，Microsoft Teams + Yuanbao + Google Chat + LINE + SimpleX 五个新消息平台，OpenAI 兼容代理把 Hermes 反过来变成"任何 OAuth 提供商的本地 API"

---

## 2. 架构层级的重大变化

### 2.1 Transport ABC：`run_agent.py` 从 11,487 行瘦身到 4,410 行

v0.11 把"格式转换 + HTTP 传输"从 `run_agent.py` 抽出，沉淀到一个新目录 `agent/transports/`。

| 文件 | 角色 |
|------|------|
| `agent/transports/base.py` | Transport ABC，定义统一接口 |
| `agent/transports/anthropic.py` | Anthropic Messages API 形态 |
| `agent/transports/chat_completions.py` | OpenAI Chat Completions 默认形态 |
| `agent/transports/codex.py` + `codex_app_server.py` | OpenAI Codex / Responses API 形态 |
| `agent/transports/bedrock.py` | AWS Bedrock Converse API 形态 |
| `agent/transports/hermes_tools_mcp_server.py` | Hermes 工具 MCP 服务端 |

为什么这是体系级变化：

1. `run_agent.py` 行数从 11,487 → 4,410（-61.6%），从"巨型主循环"变成"调度 + 工具循环 + 状态机"
2. 新增任何一种 API 形态（如未来某个 OAuth 厂商的私有协议）现在只需要再加一个 Transport 类，不必再去改主循环
3. AWS Bedrock 原生支持（Converse API）就是第一个验证这个抽象的实战案例
4. 这也是 v0.14 能轻松增加"Codex app-server runtime"作为一种额外执行后端的前提

> 注：v0.9.0 研究里第 5 章 `Agent 核心循环` 给的代码路径主要基于 `run_agent.py` 的旧形态。现在第 5 章的"三种 API 模式"应当读成"经由 Transport 接口实现的多种形态"，详见 § 6 影响矩阵。

### 2.2 Ink TUI + tui_gateway：第二条交互前端

v0.11 引入了一个完整的 React/Ink TUI（约 310 commits），是除了"经典 CLI"之外的第二条交互前端：

```
ui-tui/             # React/Ink 前端（TypeScript）
  ├── src/entry.tsx    # TTY 入口判定
  ├── src/app.tsx      # 状态机
  ├── packages/        # 子包
  └── ...
tui_gateway/        # Python JSON-RPC 后端
  ├── server.py
  ├── transport.py
  ├── slash_worker.py  # 持久化的斜杠命令子进程
  └── ...
```

调用方式：`hermes --tui` 或 `HERMES_TUI=1 hermes`

关键能力：
- 粘性 composer、流式渲染、OSC-52 跨 SSH 剪贴板
- 子 Agent spawn 可观测覆盖层（v0.11）
- LaTeX 渲染、`/reload` 热重载、可插拔的 busy 指示器（v0.12）
- `/model` 内联认证、可折叠的启动横幅、状态栏压缩计数（v0.13）
- 终端可点击 URL（OSC8）、Markdown 链接解析为页面标题（v0.14）
- 视觉端点：可"附着"到现有 gateway 而不是独立启动

性能侧：v0.12 通过 lazy agent init 把"可见冷启动"砍掉约 57%；v0.14 再补一刀 ~19 秒。

### 2.3 Curator：第一个"完全自治的后台 Agent"

v0.12 引入 `hermes curator`，作为 gateway cron ticker 上的一个后台 Agent：

- 默认 7 天一个周期
- 评分、合并、清理技能库；产出 `logs/curator/REPORT.md` + `run.json`
- "整合"与"剪枝"通过模型 + 启发式分类
- 内置防御：禁止改写 bundled / hub 技能（v0.13 用 frontmatter name 闭合一个绕过）
- 配置统一到 `auxiliary.curator`，可在 `hermes model` 里挑模型

v0.13 进一步细化：
- 新增子命令 `hermes curator archive` / `prune` / `list-archived`
- `hermes curator run` 改成同步执行，不再需要轮询
- `hermes curator status` 按使用频次排名

实现入口：`agent/curator.py`（1,781 行）+ `hermes_cli/curator.py`。

意义：原研究第 10 章把"自进化"主要描述为"用户驱动 → Skill 自动创建 → 反思"，Curator 把这条主线延展为"完全脱离用户输入的后台维护循环"。这是 v0.9.0 研究里没有的形态，详见 § 6 第 10 章影响。

### 2.4 Kanban：多 Agent 多 profile 耐久协作板

v0.13 落地的 Kanban 是研究里完全没有的新形态：

```
hermes_cli/kanban.py            # CLI 入口（2,762 行）
hermes_cli/kanban_db.py          # 存储
hermes_cli/kanban_decompose.py   # 任务拆解
hermes_cli/kanban_specify.py     # 任务细化（v0.14 用 aux LLM）
hermes_cli/kanban_swarm.py       # 多 worker 调度
hermes_cli/kanban_diagnostics.py # 卡板诊断
tools/kanban_tools.py            # Agent 端工具
plugins/kanban/                   # 仪表板插件
```

关键设计：
- 多 profile：一个 install 多个看板，跨 profile 共享工作区与日志
- Worker 可靠性：心跳 / reclaim / 僵尸检测 / 重试预算 / 幻觉网关
- 任务工具：`kanban_list` / `kanban_unblock`（v0.14）、`kanban_comment`（带作者注入防护）
- 仪表板：可视化运行、拖拽、内嵌创建、平台首页通知开关
- 编排：orchestrator 角色可生成 worker，与"orchestrator → worker"职责分离

意义：这是从"单 Agent + 子 Agent 委托"到"多 Agent 多 profile 持久协作"的范式转换。原研究第 19 章只描述了 `delegate_task` 的子 Agent 委托，Kanban 直接补上"对等多 Agent + 状态机 + 仪表板"这条新主线。

### 2.5 `/goal` / `/subgoal` / `/steer` / `/handoff`：Ralph 循环 + 实时干预原语

四个新的会话控制原语，把"用户与 Agent 的握手"细化到 turn 内：

| 原语 | 版本 | 作用 |
|------|------|------|
| `/steer <text>` | v0.11 | 在下一次 tool call 后给运行中的 agent 注入提示，不打断当前 turn、不破缓存 |
| `/goal <text>` | v0.13 | 锁定一个跨 turn 持续目标，Agent 按 Ralph 循环执行到判定完成 |
| `/handoff <target>` | v0.14 | 把当前会话（消息 / 工具调用 / 上下文）实时移交到目标 model / persona / profile |
| `/subgoal <text>` | v0.14 | 给运行中的 `/goal` 追加成功判据，由判官在原循环里折算 |

实现入口：`hermes_cli/goals.py`（762 行）。

意义：原研究第 5 章的"用户输入 → 单 turn → 工具循环 → 完成"模型，需要叠加"用户可以中途注入意图与目标"的新层。

---

## 3. 模型与 Provider 体系扩张

### 3.1 新 Provider（v0.11-v0.14）

| Provider | 版本 | 类型 | 备注 |
|---------|------|------|------|
| AWS Bedrock | v0.11 | API | 原生 Converse API，是验证 Transport ABC 的第一例 |
| NVIDIA NIM | v0.11 | API | 含计费 origin header |
| Arcee AI | v0.11 | API | 直连 |
| Step Plan | v0.11 | API | |
| Google Gemini CLI OAuth | v0.11 | OAuth | |
| Vercel ai-gateway | v0.11 | 网关 | 含定价 + 动态发现 |
| GMI Cloud | v0.12 | API | 一等公民 |
| Azure AI Foundry | v0.12 | API | 自动检测 |
| LM Studio | v0.12 | 本地 | 从"自定义 endpoint 别名"升级为一等 provider |
| MiniMax OAuth | v0.12 | OAuth | PKCE 浏览器流程 |
| Tencent Tokenhub | v0.12 | API | |
| Pluggable Provider | v0.13 | 插件 | `ProviderProfile` ABC + `plugins/model-providers/` |
| NovitaAI | v0.14 | API | |
| xAI Grok SuperGrok | v0.14 | OAuth | grok-4.3 → 1M 上下文窗口 |

### 3.2 关键新形态

**Pluggable providers（v0.13）**：以 `ProviderProfile` 为 ABC，第三方 provider 现在不必改主仓库，丢到 `plugins/model-providers/` 即可。落地后 v0.14 直接给 NovitaAI 这一类 provider 一个标准位置。当前 `plugins/model-providers/` 下有 28 个内置 provider 配置。

**OpenAI 兼容代理（v0.14）**：`hermes proxy` 起一个 `http://localhost:<port>`，对外讲 OpenAI API 协议，对内由任何已 OAuth 登录的 Hermes provider（Claude Pro / ChatGPT Pro / SuperGrok）承担。一旦上线：Codex CLI、Aider、Cline、Continue 等期望 OpenAI 兼容端点的工具都可以"借住"在 Hermes 已有的订阅之下。

入口：`hermes_cli/proxy/cli.py` + `hermes_cli/proxy/server.py` + `hermes_cli/proxy/adapters/`。

意义：把 Hermes 从"消费 OAuth 的 Agent"反过来变成"对外提供 OAuth → OpenAI API 适配的中继"。对开源生态的杠杆非常大。

**Codex app-server runtime（v0.14）**：可选 runtime，把 OpenAI Codex CLI 作为执行后端使用，带会话复用、自动退役卡死会话、OAuth refresh 分类——是把"Hermes 用 Codex"做成长跑 Agent 的工程化补丁。

**跨会话 1 小时 prompt 缓存（v0.14）**：对 Claude（Anthropic / OpenRouter / Nous Portal）开放了 1h prefix cache，跨 session、跨 `/new` 都能命中。背景 review fork 也接进缓存，避免每轮都按全量计费。

**OpenRouter Pareto Code router（v0.14）**：增加 `min_coding_score` 配置，让 OpenRouter 在满足代码质量分数下限的前提下挑最便宜的模型。

### 3.3 Provider 主选择器 UI

v0.12 把"辅助模型（compression / vision / session_search / title_generation）"独立到 `hermes model` 的专属屏幕；`auto` 路由现在默认走主模型，不再悄悄把所有人的辅助任务下放给一个低成本默认 provider。

---

## 4. 平台与入口

### 4.1 22 个消息平台（从 v0.9 的 18 涨到 22）

| 平台 | 加入版本 | 适配位置 | 备注 |
|------|----------|----------|------|
| QQBot | v0.11 (17th) | `gateway/platforms/qqbot/` | QQ 官方 API v2，QR 扫码配置 |
| Microsoft Teams | v0.12 (19th, plugin) | `plugins/platforms/teams/` | 通过 pluggable platforms 落地 |
| Tencent 元宝 | v0.12 (18th) | `gateway/platforms/yuanbao*.py` | 文本 + 媒体 |
| Google Chat | v0.13 (20th) | `plugins/platforms/google_chat/` | |
| LINE | v0.14 (21st) | `plugins/platforms/line/` | 日韩台主流 |
| SimpleX Chat | v0.14 (22nd) | `plugins/platforms/simplex/` | 去中心化隐私 IM |

另有 IRC / mattermost / ntfy / discord 同时迁入 `plugins/platforms/` 作为插件化平台示例。

### 4.2 Pluggable Gateway Platforms（v0.12）

Gateway 变成一个平台插件宿主：第三方/社区想加新 IM 平台，不必动核心，drop-in 一个适配器即可。

- 平台插件目录：`plugins/platforms/`（v0.14 已经有 8 个）
- 平台插件 hook：`env_enablement_fn`、`cron_deliver_env_var`、`standalone_sender_fn`、`pre_gateway_dispatch`、`pre_approval_request` / `post_approval_response`、`transform_llm_output`、`hook_ctx.chat_id`

### 4.3 平台层共享能力

- **Webhook 直发模式（v0.11）**：不经过 LLM，做"零 LLM 推送通知"
- **多图原生发送（v0.12）**：Telegram / Discord / Slack / Mattermost / Email / Signal 统一
- **集中化音频路由（v0.12）**：FLAC + Telegram 文档回退
- **`allowed_{channels,chats,rooms}` 跨平台白名单（v0.13）**
- **`[[as_document]]` skill 媒体路由指令（v0.13）**
- **`busy_ack_enabled` / `gateway_restart_notification` / 自动删除系统通知（v0.13）**
- **`clarify` 工具原生按钮（v0.14）**：Telegram + Discord 的多选直接渲染成 inline keyboard
- **Discord 频道历史回填（v0.14, 默认 on）**：第一次进入频道时读最近历史，避免"现在在聊什么？"
- **per-platform circuit breaker（v0.14）**：单平台失败不影响其它平台，新增 `/platform` 命令

---

## 5. 工具系统与自动化

### 5.1 工具新形态

| 工具 | 版本 | 作用 |
|------|------|------|
| `video_analyze` | v0.13 | Gemini 等多模态模型的原生视频理解 |
| `video_generate`（统一） | v0.14 | 一个工具 + 可插拔 video provider 后端 |
| `vision_analyze` 透传像素 | v0.14 | vision-capable 模型直接拿到像素而非文本摘要 |
| `x_search` | v0.14 | 一等公民的 X (Twitter) 搜索，OAuth 或 API key |
| `computer_use` cua-driver | v0.14 | 非 Anthropic 模型也能做 GUI 自动化 |
| `browser_cdp` | v0.12 | 原始 DevTools Protocol 透传 |
| `browser_console` 180x | v0.14 | 通过 supervisor 持久 CDP WS 复用连接 |
| LSP 语义诊断 | v0.14 | `write_file` / `patch` 后自动跑语言服务器，反馈错误 |
| 文件变更校验脚注 | v0.14 | 每个 turn 末尾汇总写入文件、行数变化，Agent 自我纠错 |
| Per-job `enabled_toolsets` | v0.11 | cron 每个 job 限制工具集，限 token + 限成本 |
| `no_agent` cron 模式 | v0.13 | 跳过 Agent 直接跑脚本，空输出静默，非空逐字交付 |
| `context_from` cron | v0.12-v0.13 | 链接多个 cron job 的输出 |
| Per-job `workdir` | v0.12 | 项目感知的 cron 执行 |
| `dispatch_tool` / `register_command` | v0.11 | 插件可注册斜杠命令、可主动派发工具 |
| `transform_tool_result` / `transform_terminal_output` | v0.11 | 插件可重写工具结果、终端输出 |
| `transform_llm_output` | v0.13 | 在结果落到 conversation 前再剪一刀 |
| `tool_override` | v0.14 | 插件级替换内置工具 |
| `ctx.llm` | v0.14 | 插件直接复用 Agent 的 LLM 调用栈 |
| `pre_tool_call` veto / 阻断 | v0.11 | 插件可否决工具执行 |
| Shell 钩子 | v0.11 | 任意 shell 脚本作为生命周期 hook，不需要 Python 插件 |

### 5.2 Web 与图像

- **Web search 多后端拆分（v0.13）**：`search` / `extract` / `browse` 三个能力可分别选 backend
- **SearXNG（v0.13）**：原生 search-only backend，跟 `searxng-search` 可选 skill 一起发布
- **Brave Search + DDGS（v0.14）**：两个免费 backend（Brave free tier + DuckDuckGo 抓取）
- **Image gen 可插拔（v0.11）**：`image_gen` 后端 + `openai-codex` 插件（gpt-image-2 via Codex OAuth）
- **xAI image generation provider（v0.11）**

### 5.3 沙箱与执行后端

- **Vercel Sandbox 后端（v0.12）**：`execute_code` / terminal 的又一个云端选项
- **Podman 入门支持（v0.11）**：rootless entrypoint
- **Docker 以宿主用户运行（v0.12）**：避免 root 拥有的 bind mount
- **SSH 备份不覆盖远端 home 权限（v0.12）**

---

## 6. 对原研究 32 章的影响矩阵

> 矩阵列：✅ = 大幅修订 / 🔁 = 显著补充 / 📝 = 数据更新即可 / — = 基本未变

| 章节 | 状态 | 关键变化 |
|------|------|----------|
| 总纲 | ✅ | 版本基线、统计、架构层级要更新 |
| 全网调研 | 🔁 | v0.10 起社区话题中心已经迁移到 Curator + Kanban + `/goal` + Transport |
| 01 项目全景与设计哲学 | ✅ | 平台数 18→22、Python 行数 411k→912k、文件数 886→1,898 |
| 02 启动流程与配置系统 | 🔁 | `pip install hermes-agent`、`hermes -z`、原生 Windows、`hermes update --check`、HERMES_HOME 备份 |
| 03 初级使用方法 | 📝 | 入门体验受 PyPI + 冷启动 -19s 影响；命令速查多了 `-z` |
| 04 高级使用方法 | ✅ | Curator / Kanban / `/goal` / `/subgoal` / `/handoff` / `/steer` 全是新内容 |
| 05 Agent 核心循环 | ✅ | Transport ABC 抽离，`run_agent.py` 11,487→4,410 行；三种 API 模式改成 Transport 多实现 |
| 06 System Prompt 组装 | 🔁 | `[SYSTEM:` → `[IMPORTANT:`（绕 Azure 内容过滤）；cron 注入扫描扩到 assembled prompt |
| 07 Provider 与 API 模式 | ✅ | Transport 抽象、Bedrock / Pluggable Provider / OpenAI 兼容代理 / Pareto router |
| 08 记忆系统 | 🔁 | Honcho overhaul、Hindsight `update_mode='append'` dedupe、Trigram FTS5（中日韩） |
| 09 Skill 系统 | ✅ | huggingface/skills 默认 tap、Curator 维护层、direct-URL 安装、namespaced 注册、`/reload-skills` |
| 10 自进化引擎 | ✅ | Curator 把"后台维护"独立成一等公民；review fork class-first rubric + 父级 runtime 继承 + scoped toolsets |
| 11 状态管理与 Profile 隔离 | 🔁 | Nous OAuth 跨 profile 共享、shareable profile via git、`--no-skills` 空 profile |
| 12 上下文压缩与缓存 | ✅ | 跨会话 1h 缓存、压缩 retry-on-main、`protect_first_n` 可配 |
| 13 会话持久化 | ✅ | Checkpoints v2、Trigram FTS5、auto-resume、NFS/SMB/FUSE 自动降级 |
| 14 工具系统总览 | 🔁 | LSP 语义诊断、文件变更校验脚注、统一 `video_generate`、`x_search`、`tool_override` |
| 15 终端与沙箱执行 | 🔁 | Vercel Sandbox、Podman rootless、Docker run-as-host-user、SSH 权限不覆写 |
| 16 文件与代码工具 | 🔁 | post-write delta lint（v0.13 Python/JSON/YAML/TOML）+ LSP（v0.14）+ patch did-you-mean |
| 17 浏览器自动化 | ✅ | CDP supervisor 共享持久 WS，`browser_console` 180x；LAN/localhost 自动起本地 Chromium |
| 18 MCP 集成 | ✅ | SSE 传输 + OAuth 转发 + 图像 MEDIA tag + lifecycle keepalive |
| 19 子 Agent 委托 | ✅ | Orchestrator 角色、`max_spawn_depth`、跨 Agent 文件状态协调；与 Kanban 形成新二元结构 |
| 20 定时任务 | ✅ | `no_agent` 脚本模式、`context_from` 链式、per-job `workdir` 与 `enabled_toolsets` |
| 21 Gateway 网关 | ✅ | 18→22 平台、pluggable platform host、`gateway/run.py` 9,798→18,556 行 |
| 22 插件系统 | ✅ | 7+ 个新 hook（command / dispatch / pre_tool_call / transform_tool_result / transform_terminal_output / transform_llm_output / image_gen / tool_override / ctx.llm） |
| 23 ACP IDE 集成 | 🔁 | Zed ACP Registry via `uvx`、`/steer` + `/queue`、atomic 持久化、推理元数据保留 |
| 24 安全与权限 | ✅ | redaction 默认 ON、Discord guild-scoped roles（CVSS 8.1）、WhatsApp 默认拒生人、TOCTOU 关窗、SSRF floor、sudo brute force 屏蔽 |
| 25 RL 与训练环境 | 📝 | 主要在 batch_runner 周边的 fix，宏观主线未变 |
| 26 OpenClaw 架构对比 | 🔁 | "Hermes 是 OpenClaw 的 Python 重实现"判断不变；Hermes 现在多了 OpenClaw 没有的 Curator + Kanban + Transport ABC，差异点要补 |
| 27 EvoMap Evolver 对比 | 🔁 | Curator 是 review-fork 的新延伸，但与 EvoMap 自进化主循环的对比关系基本不变 |
| 28 OpenHarness 与 JiuwenClaw 对比 | 📝 | 三种 Agent 哲学的判断不变，仅需脚注最新版本号 |
| 29 全网 Harness 实现全景 | 🔁 | 四种范式分类学（Gateway / ExecutionLoop / Orchestration / Enhancement）需新增 Kanban-style 多 Agent 协作板的位置 |
| 30 多项目 Skill 系统对比 | 🔁 | Hermes 多了 Curator 这一层，单独立项做对比就够 |
| 31 多项目记忆系统对比 | 📝 | Hermes 这边 FTS5 trigram + Hindsight dedupe 是小幅改动 |
| 32 开源伦理与启示 | 📝 | 不变 |

---

## 7. 仍存在的问题与争议

### 7.1 已显式存在的悬而未决项

- **Kanban 多 profile 协作板的扩展性**：v0.13 release 注里多次提到"hallucination gate"、"zombie worker"等，从 changelog 看仍在批量打补丁中（v0.13 + v0.14 合计 30+ 个 fix），耐久性边界尚未稳定到"可以无人值守"那一档
- **`/goal` checklist + `/subgoal` 设计反复**：v0.14 明确写到 `/goal` checklist + `/subgoal` 在窗口内被回滚（#23813），`/subgoal` 以简化形式在 #25449 重新登场——说明 Ralph 循环的判官设计还没收敛
- **Curator 的"删错技能"风险**：v0.13 多次出现"protect bundled/hub skills from Curator"的 fix（#20194 / #17562 / #17578）。说明默认配置下边界条件仍在试错
- **`computer_use` cua-driver**：曾在 v0.12 加进 3 个 PR 然后被回滚 (#16927)，到 v0.14 才以 cua-driver 形态再"re-salvage"。GUI 自动化仍是变动很大的子系统
- **PyPI 包**：v0.14 才上线 `pip install hermes-agent`，目前还在"PyPI 包 + 仓库 clone"两条路并行的过渡期，文档与 wheel 体积也仍在调
- **原生 Windows**：v0.14 自标"early beta"，且单一窗口内已经有 40+ Windows-only fix

### 7.2 体系级争议（与原研究的判断仍相容）

- **"Curator + Kanban 是不是另一种 Evolver 抄袭？"**：Curator 是 review-fork 的工程化延伸，Kanban 是多 Agent 协作板，从结构上看与 EvoMap 主循环关联较弱。本文不在此扩判，留给后续单独的章节补全
- **"Pluggable everything" 的边界**：到 v0.14，Provider / Platform / Memory / Image Gen / Video Gen / Web Search / TTS / Tool 全都是可插拔的。代价是：插件 surface 表越来越大，"内置 vs 插件"的边界越来越模糊；从 release note 看，v0.13-v0.14 已经在做"插件作者指南"这类文档补丁
- **冷启动收益的可重复性**：v0.12 自标"~57% 削减"，v0.14 自标"~19s 削减"——绝对值漂亮，但"基础环境 + 主机 IO + 网络"未给出统一基线，外部很难独立复核

### 7.3 安全侧需要警惕的地方

- **redaction 默认值反复**：v0.12 关闭默认（避免错杀 patch），v0.13 又重新打开默认（堵住 8 个 P0）。这是同一个旋钮被来回拨，意味着"什么时候开/关 redaction"在工程上仍是难题
- **YOLO 与 approval 网关**：v0.14 加了 sudo brute force 屏蔽 + 3 个 dangerous-command bypass 关闭，但仍以"近期类比 Claude Code 的工作"为依据，自身门类还在扩张
- **平台分级**：v0.13 加 `DISCORD_ALLOWED_ROLES` 跨 guild 漏洞（CVSS 8.1）、`WhatsApp` 默认拒生人，说明对外平台的"默认权限"设计是个常驻议题

---

## 8. 统计与版本数据

### 8.1 源码规模对照（v0.9.0 → main 后 v0.14.0）

| 维度 | v0.9.0 | v0.14 后 main | 增幅 |
|------|--------|---------------|------|
| Python 文件数 | 886 | 1,898 | +114% |
| Python 总行数 | 411,492 | 911,835 | +122% |
| Markdown 文件数 | 556 | 1,374 | +147% |
| 测试文件数 | 577 | 1,266 | +120% |
| 消息平台数 | 18 | 22 | +22% |
| 工具数（粗估） | 47 | 60+ | +28% |
| 工具集数 | 19 | 20+ | +5% |
| 内置 Skill 类别 | 26 | 25 顶层目录（含子类细分） | 横盘 |
| 可选 Skill 数（子目录） | 14 | 82 | +485% |
| Model providers (内置 + 插件) | ~10 | 28（在 `plugins/model-providers/`） | +180% |

### 8.2 核心文件行数变化

| 文件 | v0.9.0 | v0.14 后 | 变化 | 说明 |
|------|--------|----------|------|------|
| `run_agent.py` | 11,487 | 4,410 | **-61.6%** | Transport ABC 抽离 |
| `cli.py` | 10,033 | 15,089 | +50% | 经典 CLI 持续扩展 |
| `gateway/run.py` | 9,798 | 18,556 | +89% | 平台插件化 + 4 个新平台 |
| `hermes_cli/main.py` | 6,383 | 13,981 | +119% | 子命令大量增加 |
| `agent/curator.py` | — | 1,781 | NEW | v0.12 |
| `hermes_cli/kanban.py` | — | 2,762 | NEW | v0.13 |
| `hermes_cli/goals.py` | — | 762 | NEW | v0.13 |
| `agent/transports/` 全目录 | — | ~7,000+ | NEW | v0.11 |
| `tui_gateway/` 全目录 | — | NEW | NEW | v0.11 |
| `ui-tui/` 全目录 | — | NEW | NEW | v0.11 |
| `hermes_cli/proxy/` 全目录 | — | NEW | NEW | v0.14 |

### 8.3 累计统计

| 版本 | 自上版 commits | merged PRs | 文件变更 | 新增行 | issues 闭合 |
|------|----------------|------------|----------|--------|-------------|
| v0.10.0 | ~180 | — | — | — | — |
| v0.11.0 | 1,556 | 761 | 1,314 | 224,174 | — |
| v0.12.0 | 1,096 | 550 | 1,270 | 217,776 | — |
| v0.13.0 | 864 | 588 | 829 | 128,366 | 282（含 13 P0 + 36 P1） |
| v0.14.0 | 808 | 633 | 1,393 | 165,061 | 545（含 12 P0 + 50 P1） |
| **合计** | **~4,500** | **~2,500** | **~4,800 文件** | **~735k 行** | **800+** |

---

## 9. 三句话总结

1. **底座层**：Transport ABC + Checkpoints v2 + PyPI + 原生 Windows + 冷启动连削两刀，是把 Hermes 从"研究产物 + 巨型主循环"工程化为"PyPI 可安装、可分布式分发、可在任何平台冷启的实用品"
2. **自治层**：Curator（后台维护）+ Kanban（多 Agent 协作板）+ `/goal` `/subgoal`（Ralph 循环）+ `/handoff` `/steer`（实时干预），从"对话式 Agent"走向"持续运行、用户可中途干预的多 Agent 系统"
3. **接入层**：消息平台 18→22、Provider 大量新增、Pluggable everything、OpenAI 兼容代理把订阅打通到所有兼容客户端——Hermes 把自身定位从"一个 Agent"扩成"任意 OAuth/订阅 → 任意客户端 → 任意 IM 的中继器"

> 这三句话叠加起来：v0.10 → v0.14 是 Hermes Agent 自 v0.1 起最密集的一次形态升级——五个版本压缩到 30 天内完成，且每个版本都有大题目落地。后续研究若要继续追踪，可考虑把"自治层"单列成一个独立 Part，因为它已经不只是原研究第 10 章"自进化"能容纳的形态。

---

## 10. 与原研究的衔接

- 本文与《总纲-Hermes-Agent技术主线分析.md》是平行关系：总纲负责"为什么这样设计、本质是什么"，本文负责"v0.9 之后多出了什么"
- 各章节的具体升级说明，分散落到 Part I / Part II / Part III 各章末尾的 "升级补遗" 小节
- 数据与版本基线：本文以 v0.14.0（2026-05-16，main HEAD `2517917de` 2026-05-25）为最新参考点
- 未来追加的章节（如"Kanban 多 Agent 范式"、"Curator 与 Self-Evolve 的关系"、"Transport ABC 与四种 API 模式"）可以在 Part II 增设位置
